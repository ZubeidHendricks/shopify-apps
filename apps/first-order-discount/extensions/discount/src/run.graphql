query RunInput {
  cart {
    buyerIdentity {
      customer {
        numberOfOrders
      }
    }
  }
  discountNode {
    metafield(namespace: "$app:settings", key: "config") {
      value
    }
  }
}
