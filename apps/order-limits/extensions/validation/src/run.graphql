query RunInput {
  cart {
    cost {
      subtotalAmount {
        amount
      }
    }
    lines {
      quantity
    }
  }
  shop {
    metafield(namespace: "factory", key: "config") {
      value
    }
  }
}
