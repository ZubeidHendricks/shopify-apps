query RunInput {
  cart {
    lines {
      id
      quantity
      cost {
        amountPerQuantity {
          amount
        }
      }
    }
  }
  discountNode {
    metafield(namespace: "$app:settings", key: "config") {
      value
    }
  }
}
