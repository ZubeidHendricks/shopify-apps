query RunInput {
  cart {
    deliveryGroups {
      deliveryOptions {
        handle
        title
      }
    }
  }
  shop {
    metafield(namespace: "factory", key: "config") {
      value
    }
  }
}
