# Generated per app. `shopify app config link` fills client_id after you create
# the app in the Partner Dashboard. Placeholders are replaced by the generator.
name = "Rename Shipping"
handle = "rename-shipping"
application_url = "https://rename-shipping.example.com"
embedded = true

[access_scopes]
scopes = "write_delivery_customizations"

[auth]
redirect_urls = [
  "https://rename-shipping.example.com/auth/callback",
  "https://rename-shipping.example.com/auth/shopify/callback",
  "https://rename-shipping.example.com/api/auth/callback"
]

[webhooks]
api_version = "2025-01"

[build]
automatically_update_urls_on_dev = true
dev_store_url = ""
