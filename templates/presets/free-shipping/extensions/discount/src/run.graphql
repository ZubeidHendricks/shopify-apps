query RunInput {
  cart {
    cost {
      subtotalAmount {
        amount
      }
    }
    deliveryGroups {
      id
    }
  }
  discountNode {
    metafield(namespace: "$app:settings", key: "config") {
      value
    }
  }
}
