{
  "name": "@factory/function-kit",
  "version": "0.1.0",
  "description": "Pure-TypeScript helpers shared across Shopify Function extensions (discounts, cart/checkout validation, delivery). Runs inside the WASM sandbox — no Node APIs.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
